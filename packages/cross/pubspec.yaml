name: cross
description: Module that handles cross app database services.
publish_to: 'none'
version: 1.0.0+1

environment: 
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  cloud_firestore: ^4.15.9
  equatable: ^2.0.5
  firebase_auth: ^4.17.9
  flutter:
    sdk: flutter
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
